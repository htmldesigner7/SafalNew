// Set to empty string so Next.js Image and Link components can correctly prepend the basePath ('/safal').
// Using absolute URLs for local assets causes Next.js Image optimization to fetch from the wrong path and return 404.
export const baseUrl = '/safal';

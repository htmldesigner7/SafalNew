import nodemailer from 'nodemailer'; const transporter = nodemailer.createTransport({ host: 'mail.safalinfosoft.com', port: 587, secure: false, auth: { user: 'inquiry@safalinfosoft.com', pass: 'R2a;9*sC5$++bTG?' } }); transporter.verify((error, success) => { if (error) { console.error('Error:', error); } else { console.log('Server is ready to take our messages'); } process.exit(0); });
